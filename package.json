{
  "name": "raisul-sohan-theme",
  "version": "7.15.0",
  "private": true,
  "description": "Build step for the Raisul Sohan WordPress theme: joins src/css and src/js into the minified files the theme enqueues.",
  "scripts": {
    "build": "node scripts/build.js",
    "check": "node scripts/build.js --check"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
